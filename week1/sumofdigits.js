// get the sum of all the digits in n
// if the sum is more than one digit get the new sum of the new digits. until the sum is a one digit number
// the input will be non negative number

function digital_root(n) {
  if (n > 9) {
  }
}
